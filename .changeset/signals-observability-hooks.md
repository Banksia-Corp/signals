---
"@banksia/signals": minor
---

Implement primitive-first Reactivity Observability & Lifecycle Event Hooks (`onTrack`, `onNotify`, `onSchedule`, `onExecute`, `onBatch`, `onError`), supporting multi-registry pipelines (`createReactivityHub`), target-scoped observation (`observe`), and zero-cost fast-path performance gates.
